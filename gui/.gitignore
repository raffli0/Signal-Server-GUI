# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
venv/
.env

# App cache (downloaded DEM / SDF terrain data, regenerable)
cache/

# PyInstaller build artefacts
dist/
build/
*.spec.bak

# Built (Windows) engine binaries dropped in for packaging — not committed
packaging/engine/**/*.exe
packaging/engine/**/*.dll

